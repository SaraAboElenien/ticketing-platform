# ─────────────────────────────────────────────────────────────────────────────
# Frontend Environment Variables — apps/web
# Copy this file to .env and fill in the values for your environment.
# .env is git-ignored; this file (.env.example) is safe to commit.
# ─────────────────────────────────────────────────────────────────────────────

# ── API Base URL ─────────────────────────────────────────────────────────────
# Development: Leave empty — Vite's dev proxy forwards /api/* to http://localhost:3000
# Production:  Set to your deployed backend URL (e.g. https://api.yourapp.vercel.app)
VITE_API_URL=

# ── Google OAuth ─────────────────────────────────────────────────────────────
# The Google OAuth 2.0 client ID from Google Cloud Console.
# Must match the GOOGLE_CLIENT_ID set in apps/api/.env.
# The redirect URI is derived dynamically from window.location.origin at runtime,
# so it does NOT need to be set here -- it is always: <origin>/auth/callback/google
VITE_GOOGLE_CLIENT_ID=your-google-client-id-here
