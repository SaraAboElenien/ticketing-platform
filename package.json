{
  "name": "ticketing-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Production-grade ticketing platform backend - 100% free and open source",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "build:vercel": "npm run build --workspace=packages/shared && npx esbuild apps/api/src/handler.ts --bundle --platform=node --target=node18 --outfile=api/handler.js --external:bcrypt",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "clean": "turbo run clean",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\""
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "turbo": "^1.12.4",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}

